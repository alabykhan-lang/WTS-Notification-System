"use strict";(()=>{const CFG=window.WTS_CONFIG,$=s=>document.querySelector(s);async function load(){try{const r=await fetch(`${CFG.supabaseUrl}/rest/v1/rpc/school_notification_public_live_summary`,{method:"POST",headers:{"Content-Type":"application/json",apikey:CFG.publishableKey},body:"{}"});const d=await r.json();if(!r.ok||d?.ok===false)throw new Error(d?.code||"LIVE_SUMMARY_FAILED");const c=d.contacts||{},m=d.messages||{},delivery=d.delivery||{};if($("#mContacts"))$("#mContacts").textContent=c.active||0;if($("#mOptin"))$("#mOptin").textContent=c.whatsapp_opted_in||0;if($("#mDrafts"))$("#mDrafts").textContent=m.draft||0;if($("#mSent"))$("#mSent").textContent=m.sent||0;if($("#mFailed"))$("#mFailed").textContent=m.failed||0;const dot=$("#dot"),text=$("#connectionText"),mode=$("#modeText"),notice=document.querySelector('.notice');if(dot)dot.classList.add("on");if(text)text.textContent="Live database";if(mode)mode.textContent=delivery.enabled?(delivery.dry_run?"Delivery provider is connected in safe test mode.":"Live external delivery is enabled."):"The real queue is connected, but no live WhatsApp/SMS provider is activated yet.";if(notice)notice.innerHTML='<b>Live system:</b> this dashboard is reading the real WTS notification database. Login is required only to view contacts, create messages or change delivery settings.';}catch(e){const text=$("#connectionText");if(text)text.textContent="Live data unavailable";}}load();setInterval(load,60000);})();